kind         = "azurerm_postgresql_flexible_server_firewall_rule"
display_name = "Azure Postgresql Flexible Server Firewall Rule"
provider     = "azure"

# FREE: no per-resource Azure charge (parent or data plane bills).

[[dimensions]]
id       = "free"
label    = "Free resource"
unit     = "n/a"
quantity = "0"
rate     = "0"

[fixture]
attributes = {}
expected_monthly_cost = 0
exact = true

