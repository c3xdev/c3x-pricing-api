kind         = "azurerm_mysql_server"
display_name = "Azure MySQL Single Server (legacy)"
provider     = "azure"

# STATIC: legacy Single Server GP Gen5 $0.0438/vCore-hr + storage.

[[dimensions]]
id       = "compute"
label    = "vCores (GP Gen5)"
unit     = "vCore-hours"
quantity = "default(vcores, 2) * monthly_hours()"
rate     = "0.0438"

[[dimensions]]
id       = "storage"
label    = "Storage"
unit     = "GB-month"
quantity = "default(storage_mb, 51200) / 1024"
rate     = "0.115"

[fixture]
attributes = { vcores = 2, storage_mb = 51200 }
expected_monthly_cost = 69.7
exact = true
last_verified = "2026-06-10"
