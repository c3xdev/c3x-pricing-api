kind         = "azurerm_app_service_custom_hostname_binding"
display_name = "Azure App Service Custom Hostname Binding"
provider     = "azure"

# FREE: no per-resource Azure charge (parent or data plane bills).

[[dimensions]]
id       = "free"
label    = "Free resource"
unit     = "n/a"
quantity = "0"
rate     = "0"

[fixture]
attributes = {}
expected_monthly_cost = 0
exact = true

