kind         = "azurerm_lb_probe"
display_name = "Azure Lb Probe"
provider     = "azure"

# FREE: no per-resource Azure charge (parent or data plane bills).

[[dimensions]]
id       = "free"
label    = "Free resource"
unit     = "n/a"
quantity = "0"
rate     = "0"

[fixture]
attributes = {}
expected_monthly_cost = 0
exact = true

