kind         = "azurerm_hdinsight_hadoop_cluster"
display_name = "Azure HDInsight Cluster"
provider     = "azure"

# STATIC: D12v2-class node (VM + HDInsight surcharge) ≈ $0.519/hr;
# 2 head nodes + worker count.

[[dimensions]]
id       = "nodes"
label    = "Cluster nodes (head + workers, D12v2-class)"
unit     = "node-hours"
quantity = "(2 + default(roles_worker_node_target_instance_count, 3)) * monthly_hours()"
rate     = "0.519"

[fixture]
attributes = { roles_worker_node_target_instance_count = 3 }
expected_monthly_cost = 1894.35
exact = true
last_verified = "2026-06-10"
