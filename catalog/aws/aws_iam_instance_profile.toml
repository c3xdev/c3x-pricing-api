kind         = "aws_iam_instance_profile"
display_name = "AWS Iam Instance Profile"
provider     = "aws"

# FREE: no per-resource AWS charge (parent resource or data plane
# carries the billing).

[[dimensions]]
id       = "free"
label    = "Free resource"
unit     = "n/a"
quantity = "0"
rate     = "0"

[fixture]
attributes = {}
expected_monthly_cost = 0
exact = true

