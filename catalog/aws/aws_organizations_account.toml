kind         = "aws_organizations_account"
display_name = "AWS Organizations Account"
provider     = "aws"

# FREE: no per-resource AWS charge (parent resource or data plane
# carries the billing).

[[dimensions]]
id       = "free"
label    = "Free resource"
unit     = "n/a"
quantity = "0"
rate     = "0"

[fixture]
attributes = {}
expected_monthly_cost = 0
exact = true

