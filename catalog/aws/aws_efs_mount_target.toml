kind         = "aws_efs_mount_target"
display_name = "Efs Mount Target"
provider     = "aws"

# FREE: no per-resource AWS charge.

[[dimensions]]
id       = "free"
label    = "Free resource"
unit     = "n/a"
quantity = "0"
rate     = "0"

[fixture]
attributes = {}
expected_monthly_cost = 0
exact = true

