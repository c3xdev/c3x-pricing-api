kind         = "aws_chatbot_slack_channel_configuration"
display_name = "AWS Chatbot Slack Channel Configuration"
provider     = "aws"

# FREE: structural / wiring resource — no per-resource AWS charge.
# Data-plane fees ride on the parent or attached resources.

[[dimensions]]
id       = "free"
label    = "Free resource"
unit     = "n/a"
quantity = "0"
rate     = "0"

[fixture]
attributes = {}
expected_monthly_cost = 0
exact = true
