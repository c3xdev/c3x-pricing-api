kind         = "aws_acm_certificate"
display_name = "AWS ACM Certificate"
provider     = "aws"

# Public ACM certificates are free. Private certificate authority (ACM-PCA)
# carries a separate monthly fee and is modelled as a different resource.

[[dimensions]]
id       = "free"
label    = "ACM certificate (free for public certs)"
unit     = "n/a"
quantity = "0"
rate     = "0"

[fixture]
attributes = {}
expected_monthly_cost = 0
exact = true

