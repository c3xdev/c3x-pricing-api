kind         = "google_compute_target_pool"
display_name = "GCP Compute Target Pool"
provider     = "gcp"

# FREE: no per-resource GCP charge.

[[dimensions]]
id       = "free"
label    = "Free resource"
unit     = "n/a"
quantity = "0"
rate     = "0"

[fixture]
attributes = {}
expected_monthly_cost = 0
exact = true

