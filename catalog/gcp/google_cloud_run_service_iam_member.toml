kind         = "google_cloud_run_service_iam_member"
display_name = "GCP Cloud Run Service Iam Member"
provider     = "gcp"

# FREE: no per-resource GCP charge (parent or data plane bills).

[[dimensions]]
id       = "free"
label    = "Free resource"
unit     = "n/a"
quantity = "0"
rate     = "0"

[fixture]
attributes = {}
expected_monthly_cost = 0
exact = true

