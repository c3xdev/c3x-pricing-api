kind         = "google_compute_target_http_proxy"
display_name = "GCP Compute Target Http Proxy"
provider     = "gcp"

# FREE: no per-resource GCP charge (parent or data plane bills).

[[dimensions]]
id       = "free"
label    = "Free resource"
unit     = "n/a"
quantity = "0"
rate     = "0"

[fixture]
attributes = {}
expected_monthly_cost = 0
exact = true

