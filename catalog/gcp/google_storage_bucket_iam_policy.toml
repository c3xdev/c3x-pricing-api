kind         = "google_storage_bucket_iam_policy"
display_name = "GCP Storage Bucket Iam Policy"
provider     = "gcp"

# FREE: no per-resource GCP charge (parent or data plane bills).

[[dimensions]]
id       = "free"
label    = "Free resource"
unit     = "n/a"
quantity = "0"
rate     = "0"

[fixture]
attributes = {}
expected_monthly_cost = 0
exact = true

