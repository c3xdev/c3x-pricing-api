kind         = "google_compute_health_check"
display_name = "GCP Compute Health Check"
provider     = "gcp"

# FREE: no per-resource GCP charge.

[[dimensions]]
id       = "free"
label    = "Free resource"
unit     = "n/a"
quantity = "0"
rate     = "0"

[fixture]
attributes = {}
expected_monthly_cost = 0
exact = true

