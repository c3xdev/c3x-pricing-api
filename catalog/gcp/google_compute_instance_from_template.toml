kind         = "google_compute_instance_from_template"
display_name = "GCP Compute Instance From Template"
provider     = "gcp"

# FREE: this Terraform resource creates an instance whose attributes
# come from a referenced template. Billing flows through the resulting
# compute instance — modelled by google_compute_instance.

[[dimensions]]
id       = "free"
label    = "Free resource"
unit     = "n/a"
quantity = "0"
rate     = "0"

[fixture]
attributes = {}
expected_monthly_cost = 0
exact = true
